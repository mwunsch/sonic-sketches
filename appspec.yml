version: 0.0
os: linux
files:
  - source: /
    destination: /srv/sonic_sketches
permissions:
  - object: /srv
    owner: ubuntu
    group: ubuntu
hooks:
  AfterInstall:
    - location: script/deps
      timeout: 1200
      runas: ubuntu
